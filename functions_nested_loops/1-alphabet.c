#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - prints the alphabet
 * Return: there is no return
 */

void print_alphabet(void)
{
	char x;
		for (x = 'a' ; x = 'z' ; x++)
		{
			_putchar (x);
		}
	_putchar ('\n');

}
