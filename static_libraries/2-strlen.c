#include <stdio.h>
#include "main.h"

/**
 * _strlen - length of a string
 * @s: parameter
 * Return: return
 */

int _strlen(char *s)
{
	int x;

	for (x = 0; s[x] != '\0'; x++)
	{
		;
	}
	return (x);
}
