#include <stdio.h>
/**
 * main - Print alphabet
 * Return: Always (0)
 */

int main(void)
{
	char alpha;

	for (alpha = 48 ; alpha <= 57 ; alpha++)
		putchar ("%x", alpha);

	for alpha = 'a' ; alpha <= 'z' ; alpha++)
		putchar ("%x", alpha)

	putchar ('\n');


		return (0);
}
